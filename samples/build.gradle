description = 'Samples'
dependencies {
    compile project(':retrofit')
    compile project(':retrofit-mock')
    compile project(':converters:converter-gson')
    compile group: 'com.squareup.okhttp', name: 'mockwebserver', version: okHttpVersion
    compile group: 'com.google.guava', name: 'guava', version: guavaVersion
}
